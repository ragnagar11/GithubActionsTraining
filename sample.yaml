name: Run on File Push

on:
  push

jobs:
  run-on-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run a sample script
        run: echo "Hello, a file was pushed!"
